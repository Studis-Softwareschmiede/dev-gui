node_modules/
.env
dist/
build/
client/dist/
.claude/worktrees/
test/.tmp-*/

# lokale, host-spezifische Compose-Overrides (BOARD_ROOTS etc.) — nicht ins Repo
docker-compose.override.yml

# Metrik-Ledger (lokal, append-only; baseline.json wird separat gepflegt)
.claude/metrics/dispatches.jsonl
.claude/metrics/items.jsonl

# Feature-Drain-Laufzeitstatus (board-feature-drain.sh, lokal je Sandbox)
board/runs/
playwright-report/
test-results/
.playwright/
