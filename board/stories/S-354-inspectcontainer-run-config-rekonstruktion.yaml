id: S-354
parent: F-080
title: 'Update-Boundary: VpsDockerControl.inspectContainer + Run-Config-Rekonstruktion
  (Env/Mount/Label bleiben erhalten)'
status: Done
priority: P3
spec: docs/specs/container-image-update.md
implements:
- AC5
- AC6
depends: null
labels:
- backend
- deploy
- security
size_est: M
dispo_est: '5.0'
dispo_act: '4'
dispo_forecast: '.2500'
estimate_note: 'Heuristik M (score=6: 2 AC + 3 Komponenten [src/deploy/VpsDockerControl.js
  inspectContainer, Run-Config→Saga-Parameter-Abbildung, Tests] + security-Label).
  estimator (Task/agents/estimator.md) in diesem Lauf nicht dispatchbar (Subagent
  ohne Task-Tool) → dispo_est als Baseline-Fallback js|balanced|M ep=5.0 (n=8), confidence
  daher low. tok_est null (baseline tok_total null, Cold-Start). Fundament für S-355.
  Kern: neue Methode inspectContainer(vps, containerId, opts?) → {image, env, binds,
  labels} via „docker inspect <containerId>" über SSH. WICHTIG: getrennt von der bestehenden
  inspect(vps, image) halten — die inspiziert ein IMAGE auf ExposedPorts (deploy-lifecycle
  AC13) und wird NICHT ersetzt. Container-ID-Validierung + Shell-Escaping wie bei
  rm; geheimnisfreie Fehlerklassen. Zweiter Teil: die gelesene Run-Config auf die
  Parameter der bestehenden Deploy-Saga abbilden (containerEnv sowie requiresConfig/configApp/configMountPath
  gemäss deploy-config-volume-mount) — Hintergrund: es gibt KEINEN Deploy-State-Store
  (ADR-005), der bestehende Container ist die einzige Quelle seiner eigenen Run-Config;
  ohne diese Rekonstruktion würde ein Update Env und config-Mount still verlieren.
  Die Abbildung muss erkennen, wenn sie NICHT eindeutig ist (z.B. Binds ausserhalb
  des bekannten Verzeichnis-Mount-Musters) und das als Prädikat nach aussen geben
  — S-355 macht daraus den fail-closed-Abbruch (AC7). Floor: env kann Secrets enthalten
  (GPG_PASSPHRASE, deploy-bitwarden-gpg-injection) — server-intern/transient, NIE
  in Response/Log/Audit/WS/Frontend; keine Env-Werte in Rückgaben, die eine HTTP-Response
  erreichen können.

  '
confidence: low
tok_est: null
branch: feat/S-354-inspectcontainer
pr: null
blocked_reason: null
created_at: '2026-07-15T00:00:00Z'
updated_at: '2026-07-15T13:05:00Z'
area: deployment
done_at: '2026-07-15T13:04:42Z'
