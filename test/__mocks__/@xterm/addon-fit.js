/**
 * Manual stub for @xterm/addon-fit — loaded via moduleNameMapper in jest tests.
 */
import { jest } from '@jest/globals';

export class FitAddon {
  constructor() {
    this.fit = jest.fn();
  }
}
