## 2026-07-22 — `npm run test:worktree` (S-400/F-092, gelandet) ersetzt den Tar-Kopie-Workaround (2026-07-06/2026-07-18): direkt im Auftrags-Worktree nutzbar, kein Scratch-Copy mehr nötig

Bei S-408 lief die volle Suite direkt im `.claude/worktrees/<id>/`-Arbeitsverzeichnis via `npm run test:worktree` (`--no-cache --cacheDirectory=node_modules/.cache/jest-worktree`, `.claude/worktrees/` NICHT in `testPathIgnorePatterns`/`modulePathIgnorePatterns`) grün durch (274/274 Suiten, 8472/8472 Tests) — kein `tar`-Spiegeln nach `/tmp`, kein `node_modules`-Symlink-Risiko mehr nötig. Das Script wurde mit `worktree-friendly-tests` (F-092/S-400) genau für diesen Tester-Workflow eingeführt. Regel: bevor der Tar-Kopie-Workaround (2026-07-06-Eintrag) gezogen wird, IMMER zuerst `package.json` auf ein `test:worktree`-Script prüfen (`grep -n '"test:worktree"' package.json`) — existiert es, ersetzt es den Workaround vollständig für den Testlauf-Schritt; der Tar-Kopie-Workaround bleibt nur Fallback für Repos ohne dieses Script. `npm test` (Haupt-Gate, CI) bleibt davon unberührt — nur der lokale Worktree-Testlauf nutzt `test:worktree`. *[seen-in: S-408 red-team-kachel-rueckbau — voller Lauf direkt im Worktree ohne Tar-Kopie, 274 Suiten/8472 Tests grün inkl. der laut Review als flaky gemeldeten Suiten (RegressionDefineDialog.test.jsx, nightwatch-lock-contention.integration.test.js); promoted: 2026-07-22]*

## 2026-07-18 — Tar-Kopie-Workaround (worktree-jest-guard-Lehre 2026-07-06): `node_modules` NIE per `npm ci` gegen den Symlink auf das Haupt-Repo auffüllen — erst entkoppeln oder mit `--dry-run`/Lockfile-Diff-Check begnügen, sonst installiert `npm ci` real ins geteilte Haupt-Repo

Beim S-375-Testauftrag wurde die Scratch-Kopie wie gewohnt per `tar` gespiegelt und `node_modules` initial per Symlink auf das Haupt-Repo-`node_modules` verwiesen (schneller Vollauf ohne Neuinstallation, da `package-lock.json` gegenüber `main` unverändert war — Standard-Optimierung dieser Workaround-Kette). Der Build-Schritt verlangt aber laut Profil `npm ci` als tatsächlich auszuführenden Befehl (kein reiner Backwards-Compat-Freitext-Skip) — ein `npm ci` direkt in der Scratch-Kopie hätte über den Symlink die ECHTEN `node_modules` des Haupt-Repos gelöscht+neu installiert (`npm ci` räumt das Zielverzeichnis zuerst leer), was für jede andere parallel im Haupt-Repo laufende Session (Server, andere Test-Läufe) ein stilles Chaos ausgelöst hätte — ohne dass ein Git-Konflikt sichtbar geworden wäre (`node_modules` ist gitignored). Vor dem Ausführen bemerkt und den Symlink entfernt, stattdessen ein isoliertes, echtes `npm ci` in der Scratch-Kopie selbst laufen lassen (eigene, vollständige `node_modules`-Installation, 838 Pakete, ~6s inkl. `postinstall`/`fix-pty-perms.mjs`) — sauber getrennt vom Haupt-Repo-Zustand. Regel: sobald ein Profil-Build-Befehl (`npm ci`, `pnpm install --frozen-lockfile` o.ä.) TATSÄCHLICH gegen die Scratch-Kopie ausgeführt werden soll (nicht nur `npm test` gegen bereits vorhandene Module), MUSS der `node_modules`-Symlink vorher entfernt/durch eine echte Kopie ersetzt werden — ein Install-Befehl, der das Zielverzeichnis leert, darf NIE über einen Symlink auf den geteilten Haupt-Repo-Ordner laufen. Bleibt `package-lock.json` gegenüber `main` unverändert (per `git diff main -- package-lock.json` vorab geprüft), ist der Symlink für den reinen Test-Lauf weiterhin die schnellere Wahl — nur der Build-Gate-Schritt selbst braucht die Entkopplung. *[seen-in: S-375 deployments-gpg-subview — Symlink vor `npm ci` entfernt, isolierter Install in Scratch-Kopie (838 packages, 6s), kein Einfluss auf Haupt-Repo; promoted: 2026-07-18]*

## 2026-07-18 — Assertion-FAIL in `UsageOverlay.test.jsx` (Vollauf, Diff-fremde Datei): unmocktes `new Date().toLocaleTimeString()` + fragiler `/12.5/`-Regex (Punkt matcht `:`) kollidieren wall-clock-abhängig — Mechanismus per Quellcode-Read verifiziert statt nur "Datei nicht im Diff" angenommen

S-370-Vollauf (`test/CloudflareApiPurgeCache.test.js`/`DeployOrchestrator.test.js` u.a. touched, `UsageOverlay.test.jsx` NICHT im Diff) schlug 1/8065 fehl: `getByText(/12.5/)` fand ZWEI Treffer — den erwarteten Spend-Wert `"12.5 USD"` UND die Zeile `"Stand: 12:55:56"`, weil `UsageOverlay.jsx` `Stand: {new Date(data.generatedAt).toLocaleTimeString('de-CH')}` rendert und die Testfixture `generatedAt: new Date().toISOString()` (echte Wanduhrzeit, ungemockt) setzt — der Regex `/12.5/` hat `.` als "beliebiges Zeichen", `"12:55:56"` enthält `"12:5"` (Doppelpunkt matcht den Punkt), kollidiert also GENAU dann, wenn die reale Uhrzeit Stunde 12 + Minute 50–59 ist (hier der Fall, mehrfach reproduziert über mehrere Minuten). Statt der Diff-Abwesenheit allein zu vertrauen: Testfixture (`new Date().toISOString()` statt fixem Datum) + Komponentenquelle (`toLocaleTimeString`-Zeile) gelesen, Mechanismus exakt erklärt, isoliert 2x zu verschiedenen Zeitpunkten (12:55, 12:57) identisch reproduziert — damit zweifelsfrei wall-clock-Timing-Artefakt, keine Diff-Regression (Datei betrifft `UsageOverlay`, nichts mit Cloudflare/Deploy zu tun). Regel: bei einem Assertion-FAIL gegen einen Regex mit `.`/unspezifischem Wildcard UND einem gerenderten Zeit-/Datums-String aus ungemocktem `new Date()`: sofort auf Substring-Kollision mit der AKTUELLEN Uhrzeit prüfen (analog UUID-Substring-Lehre 2026-07-15, hier für Zeit statt Zufalls-ID) — Quellcode-Read der Testfixture + Komponente ist schneller und schlüssiger als ein Doppel-Stash-Vollauf, wenn die Zeitkorrelation offensichtlich ist. *[seen-in: S-370 deploy-cache-purge — 1/8065 Vollauf-FAIL, `UsageOverlay.test.jsx` außerhalb des Diffs, wall-clock/Regex-Kollision verifiziert, kein Einfluss auf Test-Gate; promoted: 2026-07-18]*

# Tester Lessons — dev-gui (newest first)
## 2026-07-18 — Jest 30: positionale Datei-Argumente filtern NICHT mehr wie erwartet — `jest <pattern>` läuft OHNE Fehler die KOMPLETTE Suite statt der genannten Datei(en); Pflicht: `--testPathPatterns='<regex>'` (Plural, neuer CLI-Name) explizit setzen und Suiten-Zahl in der Zusammenfassung gegenprüfen

S-365-Testauftrag empfahl `node --experimental-vm-modules node_modules/.bin/jest --forceExit <suites>` mit explizit genannten Testdateien als Pfad-Argumenten (Standard-Workaround gegen den Worktree-Selbst-Guard, Lehre 2026-07-06). Mit dem im Repo installierten Jest 30.4.1 filterten weder `jest test/usageRouter.test.js` noch `jest usageRouter` (auch mit überschriebenen `--testPathIgnorePatterns`/`--modulePathIgnorePatterns`) — der Lauf meldete am Ende `Test Suites: ... 254-257 total` (komplette Suite), OHNE Fehler oder Warnung, obwohl nur EIN Dateiname übergeben wurde. Ursache: Jest 30 hat den alten `--testPathPattern`-Mechanismus (inkl. der impliziten Positional-Arg-Filterung) durch `--testPathPatterns` (Plural, NUR als CLI-Flag, nicht per Config) ersetzt; alte `--testPathPattern` wirft immerhin einen Fehler, aber reine Positional-Args werden still ignoriert. `--listTests <pattern>` täuschte zusätzlich eine korrekte Filterung vor (listete nur die 3 gewünschten Dateien), obwohl derselbe Filter im echten Lauf nicht griff. Regel: nach JEDEM gezielten Jest-Lauf (Story-Suite-Fokus VOR dem Pflicht-Vollauf) die `Test Suites: X total`-Zeile gegen die erwartete Datei-Anzahl prüfen — weicht sie stark nach oben ab, lief der Filter nicht und das Ergebnis ist kein gezielter Beleg (auch wenn alle Tests grün sind, ist es dann schlicht der Vollauf, was den Zweck des gezielten Zwischenschritts unterläuft, aber immerhin keinen falschen PASS erzeugt). Fix: `--testPathPatterns='datei1\.test\.js|datei2\.test\.js'` (Regex-OR, Plural-Flag) statt nackter Positional-Args verwenden — vor dem Pflicht-Vollauf zusätzlich per `--version` die Jest-Major-Version prüfen, da sich CLI-Flag-Namen zwischen Majors ändern können. *[seen-in: S-365 usage-official-values — `jest test/usageRouter.test.js` lief unbemerkt die volle 254-Suiten-Suite statt der 1 genannten Datei; per `--testPathPatterns` korrigiert, kein Einfluss auf finales Gate (Vollauf ohnehin Pflicht); promoted: 2026-07-18]*

## 2026-07-16 — Story-Auftrag im Worktree: IMMER den `.claude/worktrees/<id>/`-Präfix voranstellen, wenn Spec-/Doku-Pfade wie `docs/specs/<feature>.md` gelesen werden — sonst landet man beim Haupt-Repo-Stand statt beim Working-Tree-Diff

S-326-Testauftrag nannte im ARBEITSVERZEICHNIS explizit den Story-Worktree-Pfad, die Spec-Referenz im Prompt aber (wie üblich) als repo-relativen Pfad `docs/specs/regression-run.md`. Beim ersten `Read`-Aufruf wurde dieser Pfad reflexhaft mit dem Haupt-Repo-Präfix (`/Users/alex/Git/Studis-Softwareschmiede/dev-gui/docs/specs/regression-run.md`) statt mit dem Worktree-Präfix (`.../.claude/worktrees/S-326-regression-fruehausgang/docs/specs/regression-run.md`) aufgelöst — die Datei existiert an BEIDEN Pfaden, das Read-Tool wirft daher keinen Fehler, liefert aber den ALTEN Stand (hier: AC10/AC11 fehlten komplett, weil der Spec-Nachzug-Commit nur im Worktree-Branch existierte). Erst ein `grep -n "AC10\|AC11"` gegen denselben falschen Pfad (leeres Ergebnis, obwohl das Item genau diese AC referenziert) fiel als Widerspruch auf. Regel: nennt der Auftrag ein ARBEITSVERZEICHNIS unter `.claude/worktrees/<id>/`, MUSS jeder repo-relative Pfad (Spec, Source, Test, Board-YAML) explizit mit diesem Worktree-Präfix gelesen werden — NIE mit dem Haupt-Repo-Pfad, auch wenn die Datei dort ebenfalls existiert (stiller Stand-Unterschied statt Fehler). Verifikations-Trick bei Unsicherheit: `git -C <worktree> diff main -- <datei>` zeigt sofort, ob der Worktree-Stand vom Hauptzweig abweicht — weicht er ab, aber das zuvor gelesene File zeigte keine der erwarteten Änderungen, wurde vom falschen Pfad gelesen. *[seen-in: S-326 regression-fruehausgang — erster Spec-Read traf den Haupt-Repo-Stand ohne AC10/AC11, per `git diff main` + Re-Read mit Worktree-Präfix korrigiert, kein Einfluss auf das finale Gate; promoted: 2026-07-16]*

## 2026-07-15 — Vor `npm ci --ignore-scripts` (js/R07-Lehre 2026-07-06) IMMER erst `node_modules/node-pty/prebuilds/` auf vorhandene Plattform-Prebuilds prüfen — sonst zerstört `--ignore-scripts` selbst den grünen Pfad (fehlender `chmod +x` auf `spawn-helper`)
S-355-Vollauf: `npm ci --ignore-scripts` (reflexhaft nach der 2026-07-06-Lehre „kein make/gcc in dieser Sandbox") lieferte 7834/7841 grün, aber 7 FAILs in `test/PtyManager.test.js` + `test/PtySessionRegistry.test.js` (Timeouts `current: "failed"`, leere PTY-Ausgabe) — beide Dateien vom S-355-Diff NICHT berührt. Statt vorschnell als „pre-existing/Sandbox-Timing" durchzuwinken: `node -e "require('node-pty').spawn(...)"` direkt geprobt → `posix_spawnp failed`. Ursache: `node_modules/node-pty/prebuilds/darwin-arm64/` existiert in dieser Node-26-Umgebung (kein `make`/`gcc`-Bau nötig!) — aber `--ignore-scripts` übersprang zusätzlich das harmlose, nicht-kompilierende `postinstall`-Skript `scripts/fix-pty-perms.mjs`, das nur `chmod 0o755` auf `prebuilds/*/spawn-helper` setzt (macOS-`npm`-Installs strippen das `+x`-Bit). Ohne dieses `chmod` scheitert JEDER PTY-`spawn()` mit `posix_spawnp failed`. Fix: `node scripts/fix-pty-perms.mjs` manuell nachziehen (reiner Permission-Fix, keine Kompilation, kein Risiko) → beide Suiten isoliert 54/54 grün, kompletter Vollauf danach 256/256 Suiten / 7841/7841 Tests grün. Regel: die 2026-07-06-Lehre („`--ignore-scripts` bei fehlendem `make`/`gcc`") gilt NUR, wenn tatsächlich eine native Kompilation nötig wäre — vor dem reflexhaften Einsatz von `--ignore-scripts` erst `ls node_modules/<natives-Paket>/prebuilds/` (oder `build/Release/`) prüfen: existiert ein passendes Plattform-Prebuild UND ein harmloses `postinstall` (z.B. reiner `chmod`), läuft normales `npm ci` (ohne Flag) durch und ist vorzuziehen — `--ignore-scripts` sonst selbst zur Fehlerquelle. *[seen-in: S-355 container-image-update, PtyManager/PtySessionRegistry 7 FAILs durch `--ignore-scripts`-Selbstverschulden statt Diff-Regression; promoted: 2026-07-15]*

## 2026-07-15 — Ein Assertion-FAIL gegen einen zufallsgenerierten Wert (UUID/random-Token), NUR im Vollauf, in einer vom Diff NICHT berührten Suite: Substring-Kollision des Zufallswerts prüfen, bevor FAIL gewertet wird
`test/DrainReportStore.test.js` (S-354-Vollauf, Datei vom Diff nicht berührt) schlug einmalig fehl: `expect(raw).not.toContain('abc')` (soll beweisen, dass ein entfernter `token:'abc'`-Wert nicht durchgereicht wird) traf zufällig auf die generierte `reportId` (`crypto.randomUUID()`-artig), die die Teilzeichenkette "abc" zufällig enthielt (`aabc711d-c3b2-...`) — keine echte Leck-Regression. Isoliert 5x grün, kompletter Vollauf ein zweites Mal 256/256 Suiten grün. Regel: schlägt eine Suite NUR im Vollauf (nicht isoliert reproduzierbar) mit einer `.not.toContain(<kurzer String>)`-Assertion gegen einen bekannt zufallsgenerierten Feldwert (UUID, Zufalls-ID, Timestamp-Suffix) fehl, UND die Datei ist vom Diff nicht berührt: erst prüfen, ob der geprüfte String eine plausible Teilzeichenkette des Zufallswerts selbst sein kann (kurzer alphanumerischer String, z.B. 3-4 Zeichen aus `[a-f0-9]`) — falls ja, isoliert + im vollen Wiederholungslauf verifizieren statt direkt FAIL zu werten (analog Doppel-Stash-Pflicht vom 2026-07-07-Eintrag, hier aber ohne Diff-Bezug, reiner Zufalls-Kollisions-Fall). *[seen-in: S-354 container-image-update, DrainReportStore.test.js einmaliger Vollauf-Fail durch Zufalls-UUID-Substring-Kollision mit dem geprüften Leck-String "abc"; promoted: 2026-07-15]*

## 2026-07-14 — AC3-artige "ambienter Credential-Helper"-Verifikation NIE mit dem echten Host-`HOME`/System-Git-Config fahren — `git credential fill` liest ALLE Scopes und kann einen ECHTEN Live-Token ins Tool-Output drucken

Beim Versuch, die in `workspace-mutator-credential-helper.md` AC3 beschriebene Neutralisierung zusätzlich zum Unit-Mock-Test live zu erhärten (Container/Docker in dieser Sandbox nicht erreichbar), wurde `git credential fill` in einem frischen `mktemp -d`-Repo mit einem LOKAL gesetzten Fake-Helper ausgeführt — erwartet war, dass NUR der Fake-Helper (`ambient-user`/`ambient-stale-token`) antwortet. Tatsächlich lieferte der Aufruf OHNE die `-c credential.helper=`-Neutralisierung einen ECHTEN, gültigen GitHub-App-Installation-Token (`ghs_…`) des Bots dieses Repos — weil dieser Host bereits GLOBAL/SYSTEM-weit über `gh auth setup-git` einen aktiven `credential.https://github.com.helper = !gh auth git-credential` konfiguriert hat (`ensure-gh-auth.sh`-Mechanik, s. `.claude/CLAUDE.md`), der git-seitig VOR/parallel zum lokalen Repo-Helper gezogen wird — unabhängig vom `mktemp`-Repo-Kontext. Der Live-Token landete dadurch im Klartext im Tool-Output/Transkript — ein Secret-Leak-Vorfall (verstößt gegen die globale Vorgabe „Secret-lesende Befehle maskiert ausgeben"), auch wenn die Installation-Tokens laut Architekturnotiz nur ~1h TTL haben. Der Neutralisierungs-Mechanismus selbst wurde dabei zwar korrekt bestätigt (MIT `-c credential.helper=` + `-c credential.https://github.com.helper=` lieferte `git credential fill` keinerlei Credential mehr — `fatal: could not read Username`), aber der Preis (Secret im Transkript) ist inakzeptabel.

Regel: JEDE Live-Verifikation eines Credential-Helper-Verhaltens (AC3-artige Specs) MUSS in einem vollständig isolierten Git-Environment laufen, das NICHT auf das reale `$HOME`/System-Gitconfig dieses Hosts zugreifen kann — z.B. `HOME=<frisches tmp-dir>` UND/ODER `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null` explizit im Env des `git`-Aufrufs setzen, bevor irgendein `credential fill`/`push`/`pull`-Probe-Befehl läuft — niemals ungeprüft im Ambient-Environment des Agenten selbst, das einen echten, produktiven `gh`-Login tragen kann. Falls eine solche Probe versehentlich doch einen echten Tokenwert ausgibt: sofort im Report vermerken (Vorfall + betroffener Mechanismus, NIEMALS den Tokenwert wiederholen) statt ihn stillschweigend zu ignorieren. *[seen-in: S-346 workspace-mutator-credential-helper AC3 — Live-Probe gegen `git credential fill` ohne HOME-/Gitconfig-Isolation leakte einen echten `ghs_…`-Installation-Token ins Tool-Output; promoted: 2026-07-14]*



## 2026-07-07 — Assertion-FAIL (kein Loader-Fehler) in einer vom Diff NICHT berührten Suite, nur im VOLLEN Testlauf reproduzierbar: Timing-Flake unter Sandbox-Vollast, per Doppel-Stash-Vergleich verifizieren statt annehmen
`test/headless-night-drain.integration.test.js` (`realSleep(20)` nach `child.emit('close', 0)`, prüft `activeDrainProjectPaths` sei danach leer) schlug im vollen `npm test -- --maxWorkers=2`-Lauf (221 Suiten, ~216s) reproduzierbar fehl (`Received Array ["/workspace/proj-soft-end"]` statt `[]`), lief aber **isoliert** (auch 3x direkt hintereinander) UND **zusammen mit ihrer Schwester-Suite** `NightWatchScheduler.test.js` **grün**. Das ist ein reiner Timing-Flake: die feste `realSleep(20)`-Wartezeit reicht unter der CPU-Kontention von 220 parallelen Suiten manchmal nicht aus, bis die asynchrone `close`-Handler-Kette den internen State aktualisiert hat — **kein** Code-Defekt des S-317-Diffs, der diese Datei nicht berührt. Verifikations-Pflicht (über die reine „Diff berührt Datei nicht"-Vermutung hinaus): **zweimal** `git stash` (einmal MIT, einmal OHNE den Story-Diff) + jeweils den **vollen** Lauf fahren — schlägt dieselbe Suite/Assertion **identisch auf sauberem `main` ohne den Diff** fehl, ist es zweifelsfrei vorbestehend/umgebungsbedingt (Sandbox-Ressourcenkontention), nicht diff-verursacht. Reines `git diff --stat`-Prüfen auf „Diff berührt die Datei nicht" reicht bei einem **Assertion**-Fehler (anders als bei einem Loader-/Parse-Fehler, §2a) NICHT aus, weil ein geänderter, von vielen Stellen genutzter Baustein (hier `ProjectDrain.js`, das der `NightWatchScheduler` konsumiert) prinzipiell auch eine ECHTE Timing-Regression in einer scheinbar unbeteiligten Suite auslösen könnte — der Doppel-Stash-Vollauf-Vergleich ist der einzige Beleg, der beides sicher unterscheidet. *[seen-in: S-317 feature-aware-drain — Assertion-FAIL nur unter Vollast, isoliert + auf sauberem main im Vollauf identisch reproduziert, damit als pre-existing/Sandbox-Timing-Flake gewertet statt FAIL; promoted: 2026-07-07]*

## 2026-07-06 — Wird der Tester-Lauf SELBST innerhalb eines `.claude/worktrees/<id>/`-Pfads gestartet (Story-Auftrag im eigenen Worktree statt im Hauptordner), findet Jest **0 Tests** — der Worktree-Fremd-Guard trifft den EIGENEN Pfad
`jest.config.js` schließt `.claude/worktrees/` sowohl aus `testPathIgnorePatterns` **als auch** aus `modulePathIgnorePatterns` aus (js/R07-Fix gegen fremde parallele Worktrees). Läuft der Tester-Auftrag selbst mit `cwd` unter `.claude/worktrees/<id>/` (Standard-Fall bei einem `/flow`-Story-Worktree), matcht dieser Guard **den eigenen Baum** — `jest --listTests` liefert exit 0 mit leerer Liste, ein `jest <pattern>`-Aufruf meldet `No tests found` / `0 matches`, obwohl alle Testdateien real vorhanden sind. Das ist **kein** Cache-Problem (`--clearCache` ändert nichts) und **kein** Code-Defekt — reines Pfad-Artefakt des Guards, der genau umgekehrt wirkt als beabsichtigt, sobald der eigene Arbeitsordner selbst ein Worktree ist. Pflicht-Workaround vor jedem `Test-Gate: FAIL` wegen „0 Tests gefunden": Working-Tree-Stand (inkl. uncommitteter Änderungen; `git status --short`/`git diff` vorher prüfen) per `tar`-Kopie (kein `rsync` in dieser Sandbox) in ein Verzeichnis AUSSERHALB von `.claude/worktrees/` spiegeln (z.B. `/tmp/<id>-test-copy`, `node_modules` per Symlink auf den Original-Worktree statt neu installieren), dort `jest` laufen lassen, danach die Kopie wieder löschen (reine Lese-Verifikation, keine Schreiboperation im echten Repo). *[seen-in: S-303 cockpit-declutter — `jest --listTests` lieferte 0 Treffer trotz 210 realer Testdateien, weil der Auftrags-Worktree selbst unter `.claude/worktrees/flow-s303/` lag; promoted: 2026-07-06]*

## 2026-07-06 — Sandbox ohne `make`/`gcc`: `npm ci` scheitert an `node-pty` (natives Addon) — `--ignore-scripts` umgeht das, hinterlässt aber `pty.node`-Fehler in PTY-/WS-Gateway-Testsuiten (kein Code-FAIL)
In dieser Sandbox fehlen `make`/`gcc`/`g++`/root-Paketinstallation — ein normales `npm ci` bricht beim `node-gyp rebuild` von `node-pty` (nativem Addon, `postinstall`-Skript) mit `not found: make` ab, `node_modules` bleibt leer. Workaround: `npm ci --ignore-scripts` installiert alle JS-Pakete erfolgreich (810/811), lässt aber `node-pty` ohne kompiliertes `pty.node` zurück. Folge: JEDE Testsuite, die `node-pty` transitiv importiert (`PtyManager.test.js`, `SshPtyManager.test.js`, `WsGateway*.test.js`, `slugResolver.test.js`, `FlowRunner.test.js`, `ReconciliationJob.test.js`, `VpsViewDelete.test.jsx`, `githubAppToken.test.js` je nach Import-Kette) schlägt mit `Failed to load native module: pty.node, checked: build/Release, build/Debug, prebuilds/linux-x64` fehl — ein Umgebungsartefakt dieser Sandbox, KEIN Code-FAIL, wenn der Story-Diff keine dieser Dateien berührt. Zusätzlich beobachtet: `AccessGuard.test.js` (JWKS-Fail-closed-Tests, 10/27 rot) ist in dieser Sandbox AUCH auf unverändertem `main` rot (per `git stash` verifiziert — vermutlich fehlender Außennetz-Zugriff für JWKS-Fetch) — ebenfalls pre-existing, nicht diff-bezogen. UND: die volle Suite mit Standard-Worker-Zahl killt vereinzelt Jest-Worker per SIGKILL (Ressourcen-Kontention) — isoliert mit `--maxWorkers=2` gegen genau diese Suiten nachlaufen lassen, bevor `FAIL` gewertet wird. Pflicht-Vorgehen: (1) bei `npm ci`-Scheitern an einem nativen Addon-Build in dieser Sandbox `--ignore-scripts` nutzen; (2) `pty.node`-Fehler in einer vom Diff NICHT berührten Datei nie als FAIL werten; (3) vor jedem `AccessGuard`-Rot-Befund per `git stash`/Worktree-Vergleich gegen `main` prüfen, ob es pre-existing ist; (4) SIGKILL-Worker-Abbrüche isoliert mit reduzierter Worker-Zahl erneut fahren, bevor FAIL gemeldet wird. *[seen-in: S-299 repo-size-badge Frontend — reines Frontend-Item, alle drei genannten Fehlerkategorien pre-existing/Sandbox-bedingt, nicht vom Diff verursacht; promoted: 2026-07-06]*

## 2026-07-03 — Reaktivierte, zuvor „ungenutzt/tote" `onNavigate`-Prop-Kette: gegen die REALE App-Route testen, nicht nur mit einem `jest.fn()`-Spy im isolierten Komponenten-Test
Wird eine Prop reaktiviert, die laut Vorgänger-Kommentar zuvor „nur für Signatur-Kompatibilität erhalten, aber ungenutzt" war (hier: `SpecView`/`CockpitView` `onNavigate`, obsidian-sync-trigger AC6), reicht ein isolierter Komponenten-Test mit `onNavigate: jest.fn()` NICHT, um AC6/AC-Navigate-Verhalten zu beweisen — der Spy zeigt nur, DASS die Funktion aufgerufen wurde, nicht WAS sie in der echten App bewirkt. Konkret durchgerutscht (S-252): `onNavigate('factory')` ist bei Aufruf aus `SpecView`/`CockpitView` in Wahrheit `useHashRouter().navigate`, das per `viewToHash('factory') → '#/factory'` den Repo-Teil DROPT — anders als `navigateFactory(repo)`, das `#/factory/<repo>` erhält. Ein Klick auf „Starten" nach 202 wirft den Nutzer folglich auf die Repo-Übersicht zurück statt im „Terminal-/Arbeiten-Bereich" DESSELBEN Projekts zu bleiben (AC6 verlangt Letzteres). Per Ad-hoc-Integrationstest (volle `AppShell` gerendert, `window.location.hash = '#/factory/<repo>'`, echten Klick-Flow bis 202 durchgespielt, `window.location.hash` + Body-Inhalt danach geprüft) reproduziert: Hash landet bei `#/factory` (ohne Repo), Body zeigt „Fabrik — Projekt wählen" statt der Cockpit-Tableiste. Pflicht-Checkliste bei jeder Reaktivierung einer zuvor als „ungenutzt" markierten Callback-Prop, die mehrere Komponentenebenen durchreicht: einmal den vollen Baum ab dem Router-Root rendern (nicht nur die Blatt-Komponente mit gemocktem Callback) und das TATSÄCHLICHE Ergebnis (Hash/URL/gerenderter Inhalt) nach dem Callback-Aufruf prüfen — Spy-Aufruf allein beweist nichts über die Wirkung. *[seen-in: S-252 obsidian-sync-trigger — Test-Gate FAIL, AC6 via Ad-hoc-AppShell-Integrationsprobe widerlegt; promoted: 2026-07-03]*

## 2026-07-02 — routerLoader.test.js im Worktree-Pfad: `.claude/worktrees/`-Guard-Failures sind Location-Artefakt, kein FAIL
Wird die volle Jest-Suite in einem `/flow`-Worktree unter `.claude/worktrees/<id>/` gefahren, schlagen die `routerLoader.test.js`-Tests fehl, die **temporäre Router-Dirs** anlegen (`test/.tmp-router-*`, `.tmp-empty-*`, `.tmp-nonjs-*`, `.tmp-deps-*`) und via `mountRouters()` laden: der Loader-Guard (`src/routerLoader.js`) wirft `routerLoader: Verbotener Scan-Pfad: …/.claude/worktrees/…`, weil der **absolute** Temp-Pfad das verbotene Segment `/.claude/worktrees/` enthält. Das ist ein **deterministischer Location-Guard**, kein Code-Defekt und **unabhängig vom Story-Diff** — im Haupt-Repo-Pfad (ohne `.claude/worktrees/`) triggert er nicht. Vorgehen vor `FAIL`: (1) prüfen, dass **alle** Failures dieser eine Guard-String sind und nur in `routerLoader.test.js` liegen; (2) prüfen, dass die vom Diff **tatsächlich berührten** Assertions derselben Datei grün sind (z.B. Router-Count + Erwartungsliste im Describe „Echte src/routers/ Module" — laufen gegen `readdir(src/routers)`, nicht `mountRouters`, und passen). Trifft beides zu → nicht als Code-FAIL werten. `/flow` deklariert dieses Artefakt bereits im Handoff. *[seen-in: S-245 obsidian-vault-config, 9/16 routerLoader-Tests rot im Worktree, S-245-Assertions grün; promoted: 2026-07-02]*

## 2026-06-13 — Format-/Parsing-AC: gegen reale Quelldaten smoken, nicht nur gegen Fixtures
Verarbeitet ein Item eine **externe reale Quelldatei** (Ledger, Config, Markdown-Tabelle, Log), darf `Test-Gate: PASS` NICHT allein auf Unit-Tests mit hand-geschriebenen Fixtures beruhen — die beweisen nur die Format-Annahme des coders, nicht das echte Format. Konkret durchgerutscht: `RetroReader` (#175) parste die `Pack/Skill`-Spalte mit `startsWith('agents/')`; 84 Tests grün, aber das reale LEARNINGS.md umschließt Pfade mit Backticks (`` `agents/cicd.md` + `knowledge/cicd.md` ``) → live waren **alle Kategorie-`counts` 0**, der Defekt fiel erst im Rollout-Smoke auf (→ #179). Pflicht für Format-/Parsing-AC: zusätzlich zum Unit-Lauf einen **read-only Smoke gegen die echte Quelldatei** fahren (Boundary gegen die reale Datei im Plugin-Cache laufen lassen ODER nach dem Build den Endpunkt gegen Echtdaten curlen) und prüfen, dass die abgeleiteten Werte **plausibel ≠ leer/0** sind, wo die Quelle Daten hergibt. Schwester-Lesson: 2026-06-10 (jsdom-PASS ≠ Beweis) — gleiches Prinzip auf der Test-Gate-Ebene. *[seen-in: #175 Fixtures grün / #179 Echtformat-Defekt live; promoted: 2026-06-13]*

## 2026-06-10 — UI-Layout/Scroll/Overflow: jsdom-PASS ist KEIN Beweis — real im Browser verifizieren
`npm test` (jsdom) hat **keine Layout-Engine**: es kennt nur gesetzte Style-Properties, nicht `clientHeight`/`scrollHeight`/tatsächliches Scrollen oder Sichtbarkeit. Bei Items, deren AC ein **Layout-/Scroll-/Overflow-/Sichtbarkeits-Verhalten** beschreibt (Detail-Pane scrollbar, sticky, z-index, Truncation, responsive Stacking), darf `Test-Gate: PASS` NICHT allein auf jsdom-Style-Asserts beruhen — die können grün sein, während die App real kaputt ist. Konkret durchgerutscht: team-detail-scroll #147 (`flexWrap:'wrap'` → Detail-Pane wuchs am Inhalt, `overflow:hidden` clippte; jsdom-Asserts `overflowY==='auto'`/`minHeight==='0'` grün, App unscrollbar — Bug wurde gemerged UND ausgerollt). Pflicht für solche AC (siehe team-detail-scroll AC6): gegen den **gebauten** App-Stand real messen — `npm run build` + `node server.js` lokal (AccessGuard via `DEV_NO_ACCESS=1`), dann headless Chrome (puppeteer-core gegen System-Chrome, ephemer in /tmp) zur View navigieren und `scrollHeight > clientHeight` (canScroll) sowie nach `scrollTop=max` `endVisible` prüfen. Vorher/Nachher dokumentieren. *[seen-in: #147 jsdom-grün aber App-Scroll kaputt → v2 #155 grid + Browser-Messung; promoted: 2026-06-10]*
