id: S-349
parent: F-079
title: 'config-Mount-Korrektur Backend: run() generischer rw-Verzeichnis-Mount + mkdir
  -p, config-Seed-Mechanik (pushAppConfigFile/Guard) rückbauen'
status: To Do
priority: P2
spec: docs/specs/deploy-config-volume-mount.md
implements:
- AC1
- AC2
- AC3
- AC4
- AC5
- AC9
depends: null
labels:
- backend
- deploy
- security
size_est: L
dispo_est: '5.0'
dispo_act: null
dispo_forecast: null
estimate_note: 'Heuristik L (score=11: 6 AC + ~4 Komponenten VpsDockerControl.run/buildConfigHostPath
  + Entfernung pushAppConfigFile + DeployOrchestrator-Gate-Rückbau + Tests + security-Label).
  estimator (Task/agents/estimator.md) in diesem Lauf nicht dispatchbar → dispo_est
  als Baseline-Fallback js|balanced|L ep=5.0 (n=5), confidence daher low. tok_est null
  (baseline tok_total null, Cold-Start). Kern: run() legt bei requiresConfig ~/apps/<app>/config
  per mkdir -p an (leer, nie befüllt) und mountet es read-WRITE (kein :ro) nach /app/config
  (Default, optional configMountPath) zwischen envArgs und -p — GETRENNTES Escaping
  der Pfade, Slug- + configMountPath-Validierung (config-app-invalid/config-mount-path-invalid).
  Rückbau: pushAppConfigFile ersatzlos entfernen, DeployOrchestrator config-Seed-Gate
  (config-file-missing) + configSeed-Param entfernen, run()-Durchreichung auf requiresConfig/configApp/configMountPath
  umstellen. Tests (VpsDockerControl.test.js, DeployOrchestrator.test.js) entsprechend
  anpassen/entfernen.

  '
confidence: low
tok_est: null
branch: null
pr: null
blocked_reason: null
created_at: '2026-07-15T00:00:00Z'
updated_at: '2026-07-15T00:00:00Z'
area: deployment
